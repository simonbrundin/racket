apiVersion: db.atlasgo.io/v1alpha1
kind: AtlasSchema
metadata:
  name: atlasschema-plan
spec:
  urlFrom:
    secretKeyRef:
      key: uri
      name: plan-db-admin
  schema:
    configMapKeyRef:
      key: schema.sql
      name: atlas-schema
  schemas:
    - "public"
