apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base

namespace: plan-prod

# Lås prod-taggen (CI kan uppdatera detta vid release)
images:
  - name: simonbrundin/plan
    newTag: "0.0.11"

patches:
  - path: patch-replicas.yaml
  # Lägg fler patchar för resursgränser, env, hostnames etc vid behov
